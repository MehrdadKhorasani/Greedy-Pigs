function isDubble(firstNumber, secondNumber) {
  return firstNumber === secondNumber ? true : false;
}

function isSeven(firstNumber, secondNumber) {
  return firstNumber + secondNumber === 7 ? true : false;
}

function isDubbleOne(firstNumber, secondNumber) {
  return firstNumber === 1 && secondNumber === 1 ? true : false;
}
